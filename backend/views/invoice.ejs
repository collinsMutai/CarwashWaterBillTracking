<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Weekly Invoice</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 40px; }
    h1 { text-align: center; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { border: 1px solid #ddd; padding: 8px; }
    th { background-color: #f2f2f2; }
    .right { text-align: right; }
  </style>
</head>
<body>
  <h1>Weekly Invoice</h1>
  <p><strong>Week:</strong> <%= weekStart.toDateString() %> - <%= weekEnd.toDateString() %></p>

  <table>
    <thead>
      <tr>
        <th>Vehicle Registration</th>
        <th>Client Name</th>
        <th>Description</th>
        <th>Service Fee</th>
        <th>Number of Washes</th>
        <th>Total Service Fee</th>
      </tr>
    </thead>
    <tbody>
      <% services.forEach(function(svc) { %>
        <tr>
          <td><%= svc.registration %></td>
          <td><%= svc.clientName || '' %></td>
          <td><%= svc.description || '' %></td>
          <td class="right">KES <%= svc.serviceFeePerWash.toFixed(2) %></td>
          <td class="right"><%= svc.numberOfWashes %></td>
          <td class="right">KES <%= svc.totalServiceFee.toFixed(2) %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <p style="text-align: right; margin-top: 20px;">
    <strong>Water Cost per Unit:</strong> KES <%= waterCostPerUnit %>
  </p>
</body>
</html>
